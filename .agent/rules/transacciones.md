---
trigger: always_on
---

todas las transacciones deben manejarse desde el modelo o modelos... en su defecto podrias realizar la transaccion desde el controlador si es necesario pero lo correcto es hacerla desde el modelo